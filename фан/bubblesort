void  BubbleSort(int[]a,int i,int j)
{
        for (int l = 0; l < j; l++)
        {
            for (int m = l + 1; m < j; m++)
            {
                if (a[l] < a[m]) { int k; k = a[l]; a[l] = a[m]; a[m] = k; }
            }
        }
}
