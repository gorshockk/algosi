void  BubbleSort(int[]a,int i,int j)
{
        for (int l = 0; l < j; l++)
        {
            for (int m = l + 1; m < j; m++)
            {
                if (a[i, l] < a[i, m]) { int k; k = a[i, l]; a[i, l] = a[i, m]; a[i, m] = k; }
            }
        }
}
